// // ErrorHandler.tsx

// import { toast } from "sonner";

// // const isErrorObject = (obj: any): obj is ErrorObject => {
// //   return obj && (obj.error !== undefined || obj.message !== undefined);
// // };

// const SuccessHandler = ({ data }: { data?: any }) => {
//   let dataMessage = "An data occurred.";

//   if (data?.data?.message) {
//     dataMessage = data?.data?.message;
//   }

//   // Show toast with data message
//   return <>{toast.success(errorMessage)}</>;
// };

// export default SuccessHandler;
